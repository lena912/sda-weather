package com.sda.weather;

public class LocationService {

//    Location createLocation(String name, String region, String country, Double longitude, Double latitude) {
//        if (name == null || name.isBlank()) {
//            throw new IllegalArgumentException("Pole nie może być puste");
//        }
//        if (region == null || region.isBlank()) {
//            throw new IllegalArgumentException("Pole nie może być puste");
//        }
//        if (country == null || region.isBlank()) {
//            throw new IllegalArgumentException("Pole nie może być puste");
//        }
//        if (longitude == null || longitude.isNaN()) {
//            throw new IllegalArgumentException("Należy podać wartość liczbową");
//        }
//        if (latitude == null || latitude.isNaN()) {
//            throw new IllegalArgumentException("Należy podać wartość liczbową");
//        }
//
//        Location location = new Location(name, region, country, longitude, latitude);
//
//    }
}
